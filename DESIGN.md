# ArenaPilot 当前设计文档

## 1. 项目概述

ArenaPilot-斗兽塔助手是一个独立开发的 Dalamud 插件，用于自动执行已支持斗兽奇盘的固定路线和事件流程。内部名称仍为 `ArenaPilot`，以保持程序集、配置目录和插件加载标识稳定。

当前插件版本为 `0.2.4.12`。UI、程序集、插件清单和发布标签均使用相同四段版本号。

当前实现目标是完成以下闭环：

```text
劳妲入口
  -> 选择第一盘
  -> 点击挑战
  -> 点击出发
  -> 进入第一盘区域
  -> 按固定路线移动
  -> 处理战斗、休息、宝箱和商店
  -> 处理最终 Boss
  -> 等待并关闭结算
  -> 离开副本区域
  -> 回到入口并开始下一轮
```

本项目使用独立的数据模型和实现，不引用其他插件的私有程序集、私有字段、授权逻辑、签名、密钥或专有发布资源。

## 2. 当前目录

重命名完成后，项目目录应为：

```text
E:\git\ArenaPilot\
```

当前源码目录中的主要结构：

```text
ArenaPilot/
  ArenaPilot.csproj
  ArenaPilot.json
  Plugin.cs
  Configuration.cs
  Services.cs
  README.txt
  DESIGN.md
  ArenaSpec.md
  WORKLOG.md

  Actions/
    AddonUi.cs
    BattleAction.cs
    BossAction.cs
    EntryAction.cs
    PartySetupAction.cs
    PromptAction.cs
    RestAction.cs
    ResultAction.cs
    RewardAction.cs
    ShopAction.cs
    TreasureAction.cs

  Diagnostics/
    SnapshotExporter.cs

  Integration/
    GameCommandService.cs
    VnavmeshClient.cs

  Reader/
    ArenaUiReader.cs

  Runtime/
    ArenaController.cs
    ArenaState.cs
    ArenaStageRoute.cs
    ArenaRoutes.cs
    FirstArenaRoute.cs
    SecondArenaRoute.cs
    ThirdArenaRoute.cs
    FourthArenaRoute.cs
    FifthArenaRoute.cs
    CrucibleItemCatalog.cs
    ShopCatalogCollector.cs

  UI/
    MainWindow.cs
    ui-preview.html
```

`output/` 和 `obj/` 是构建产物及中间文件，不属于源码设计的一部分。重命名目录时应整体移动项目目录，不要单独移动 `output/`。

## 3. 插件入口和服务

### 3.1 Plugin

`Plugin.cs` 负责：

- 创建并注入 Dalamud 服务。
- 创建 `ArenaUiReader`、`ArenaController` 和 `MainWindow`。
- 注册 `/斗兽塔` 命令。
- 注册 Framework Update 回调。
- 在插件卸载时解除事件和释放资源。

Dalamud 可能在后台线程构造或卸载插件实例，因此 `Plugin` 和 `ArenaController` 构造/释放路径不得读取 `ObjectTable`、`GameGui`、Addon 或其他要求游戏主线程的状态。控制器构造时创建安全的空快照，并在第一次 `Framework.Update` 主线程回调中读取真实快照；首次更新前 UI 会显示“等待首次主线程状态读取”。卸载时只解除回调、停止状态机和导航，不刷新游戏快照。

### 3.2 Services

`Services.cs` 集中保存运行时使用的服务，包括：

- `IClientState`。
- `IObjectTable`。
- `ITargetManager`。
- `IChatGui`。
- `IFramework`。
- Dalamud 日志服务。

所有动作类通过 `DalamudApi` 访问服务，控制器负责协调动作，不把 UI 逻辑放入动作类。

## 4. 状态读取模型

`ArenaUiReader` 将当前客户端状态转换为独立的 `ArenaSnapshot`：

- 当前阶段 `ArenaPhase`。
- 关卡编号 `StageId`。
- 副本编号 `ContentId`。
- 区域编号和名称。
- 玩家坐标。
- 当前棋盘节点及节点类型。
- 当前 Addon 和可见 Addon。
- 阶段说明和失败原因。
- Addon 的匿名值快照。

读取器只负责观察和归类，不直接执行点击、移动或战斗操作。

### 4.1 阶段识别优先级

第一盘区域的阶段识别顺序如下：

```text
Result
  -> ItemDispose
  -> Treasure
  -> Shop
  -> Loot
  -> PartySetup
  -> Rest
  -> Battle
  -> Board
  -> BattlePreparation
  -> Loading
```

结算、宝箱、商店和奖励界面必须优先于棋盘判断，否则棋盘 HUD 可能被误识别为普通 Board 状态。

## 5. 总流程控制器

`ArenaController` 是核心协调器，负责：

- 启动、继续和停止总流程。
- 每次 Framework Update 推进流程。
- 保存当前节点、目标节点和已处理节点。
- 调用入口、移动、编队、战斗、奖励和结算动作。
- 管理超时、重试和状态提示。
- 输出流程日志和诊断状态。

主要运行变量：

| 变量 | 用途 |
|---|---|
| `fullFlow` | 是否正在运行总流程 |
| `targetNode` | 当前计划前往的节点 |
| `eventNode` | 当前正在处理事件的节点 |
| `processedNodes` | 本轮已经完成的节点 |
| `challengeSent` | 是否已发送挑战操作 |
| `challengeConfirmed` | 是否已确认挑战提示 |
| `battleObserved` | 是否观察到正式战斗状态 |
| `rewardRequested` | 是否已经请求领取战斗奖励 |
| `waitingForAreaExit` | 结算关闭后是否等待离开副本区域 |
| `wasInResult` | 用于识别 Result 到 Loading/Board 的阶段切换 |

每次动作都使用 `nextFullFlowActionUtc` 限制执行频率，避免在同一界面连续发送操作。

节点事件可能在角色进入节点中心识别半径前打开。事件出现时优先绑定当前导航目标节点；事件结束并返回棋盘后，只要对应节点已观察到战斗、商店、宝箱或休息完成状态，就直接标记该节点完成，不会到达坐标后再次等待同一事件。

## 6. 第一盘固定路线

当前第一盘区域：

- Territory ID：`1339`
- Content ID：`1088`
- 所有节点识别半径：`1.25` 米

节点路线：

```text
0 -> 1 -> 3 -> 5 -> 6 -> 7 -> 8 -> 9 -> 11 -> 12
```

节点信息：

| 节点 | 坐标 | 类型 | 默认下一节点 |
|---:|---|---|---:|
| 0 | `(-700, 0, 0)` | Start | 1 |
| 1 | `(-700, 0, -9)` | Battle | 3 |
| 2 | `(-705, 0, -16)` | Battle | 4 |
| 3 | `(-695, 0, -16)` | Battle | 5 |
| 4 | `(-705, 0, -25)` | Rest | 6 |
| 5 | `(-695, 0, -25)` | Treasure | 6 |
| 6 | `(-700, 0, -33)` | Battle | 7 |
| 7 | `(-700, 0, -42)` | Treasure | 8 |
| 8 | `(-700, 0, -51)` | Battle | 9 |
| 9 | `(-705, 0, -58)` | Treasure | 11 |
| 10 | `(-695, 0, -58)` | Rest | 11 |
| 11 | `(-700, 0, -66)` | Shop | 12 |
| 12 | `(-700, 0, -75)` | Boss | 终点 |

所有节点统一使用 `1.25` 米识别半径，避免角色距离节点中心仍有约2米时被提前判断为到达节点。节点12同样必须接近 `(-700, 0, -75)` 后才会被识别为到达 Boss 格。

### 6.1 高段第一盘固定路线

高段第一盘区域：

- Stage ID：`4`
- Territory ID：`1342`
- Content ID：`1091`
- 节点识别半径：`1.25` 米

固定路线：

```text
0 -> 1 -> 3 -> 4 -> 5 -> 7 -> 9 -> 11 -> 13 -> 14
```

节点信息：

| 节点 | 坐标 | 类型 | 默认下一节点 |
|---:|---|---|---:|
| 0 | `(-700.00, 0, 0.00)` | Start | 1 |
| 1 | `(-700.07, 0, -8.70)` | Battle | 3 |
| 2 | `(-704.93, 0, -16.60)` | Battle | 4 |
| 3 | `(-695.18, 0, -16.52)` | Random | 4 |
| 4 | `(-699.92, 0, -23.86)` | Shop | 5 |
| 5 | `(-704.86, 0, -31.34)` | Rest | 7 |
| 7 | `(-700.08, 0, -38.98)` | Battle | 9 |
| 9 | `(-694.96, 0, -46.56)` | Treasure | 11 |
| 11 | `(-694.97, 0, -55.49)` | Battle | 13 |
| 13 | `(-695.07, 0, -64.41)` | Shop | 14 |
| 14 | `(-699.12, 0, -70.80)` | Boss | 终点 |

节点2已收录坐标并连接到节点4，但不在当前首选路线中；其战斗目标 BaseId 为 `19641`。路线图按棋盘图片补齐节点6、8、10和12及全部分支连接；这些节点尚无实测坐标，以灰色仅展示节点显示，不能导航或加入自定义路线。节点3、6、8和12按 `Random` 处理，节点7为 `Battle`。

### 6.2 高段第二盘固定路线

高段第二盘区域：

- Stage ID：`5`
- Territory ID：`1343`
- Content ID：`1092`
- 节点识别半径：`1.25` 米

当前固定路线：

```text
0 -> 3 -> 4 -> 7 -> 10 -> 11 -> 14 -> 15 -> 16 -> 19 -> 18 -> 21 -> 24 -> 26
```

节点信息：

| 节点 | 坐标 | 类型 | 默认下一节点 |
|---:|---|---|---:|
| 0 | `(-700.00, 0, 0.00)` | Start | 3 |
| 2 | `(-708.922, 0, -9.023)` | Battle | 非默认分支 |
| 3 | `(-700.02, 0, -8.65)` | Battle | 4 |
| 4 | `(-691.05, 0, -9.00)` | Battle | 7 |
| 7 | `(-690.91, 0, -18.03)` | Battle | 10 |
| 8 | `(-709.009, 0, -26.963)` | Battle | 非默认分支 |
| 9 | `(-699.934, 0, -26.927)` | Battle | 非默认分支 |
| 10 | `(-690.92, 0, -26.62)` | Battle | 11 |
| 11 | `(-700.01, 0, -35.93)` | Battle | 14 |
| 13 | `(-708.922, 0, -44.974)` | Battle | 非默认分支 |
| 14 | `(-700.00, 0, -45.12)` | Rest | 15 |
| 15 | `(-691.10, 0, -44.99)` | Shop | 16 |
| 16 | `(-682.16, 0, -44.97)` | Treasure | 19 |
| 17 | `(-717.997, 0, -54.008)` | Battle | 非默认分支 |
| 18 | `(-699.88, 0, -54.00)` | Battle | 21 |
| 19 | `(-681.97, 0, -53.81)` | Battle | 18 |
| 20 | `(-708.969, 0, -62.909)` | Battle | 非默认分支 |
| 21 | `(-700.04, 0, -63.06)` | Battle | 24 |
| 23 | `(-709.112, 0, -71.555)` | Battle | 非默认分支 |
| 24 | `(-699.94, 0, -71.78)` | Rest | 26 |
| 25 | `(-690.842, 0, -72.111)` | Battle | 非默认分支 |
| 26 | `(-700.00, 0, -80.00)` | Boss | 终点 |

高段第二盘路线图同时显示图片中节点 `0-26` 的完整分支结构。未实测节点使用相对布局坐标并标记为“仅展示”：不会参与位置识别，不能加入自定义路线，也不会用于导航；补录真实坐标后才能启用。该盘独立使用 `MaxX=-680`、`MinZ=-82` 的棋盘边界，以覆盖右侧节点16/19和最终节点26。

### 6.3 第三盘固定路线

第三盘区域：

- Stage ID：`3`
- Territory ID：`1341`
- Content ID：`1090`
- 节点识别半径：`1.25` 米

默认路线避开实测中“很难打”的节点5：

```text
0 -> 1 -> 2 -> 3 -> 4 -> 6 -> 8 -> 10 -> 11 -> 12 -> 14 -> 15 -> 16
```

完整节点和分支已加入路线图。节点3提供商店/困难战斗分支，节点8提供宝箱/商店分支，节点12提供随机/休息分支；按棋盘红线默认分别选择节点4、节点10和节点14。第三盘的主要战斗目标优先级包含鱼人、尤弥尔之壳、尤弥尔、祖、拉哈穆、塞壬和最终 Boss 加特勒；公雏鸟、母雏鸟、祖的蛋、巨像、蹒跚鬼和爬行鬼不加入目标列表。

### 6.4 第二盘路线图

第二盘节点 `1-13` 坐标及类型已实测并启用节点识别；节点0为起点。默认路线为 `0 -> 1 -> 2 -> 3 -> 5 -> 7 -> 8 -> 9 -> 11 -> 12 -> 13`，在节点2选择节点3分支、节点9选择节点11分支。节点3、7、9、10、13 的 BOSS BaseId 已加入默认自动选中优先级；奇子·塔纳托斯（`19748`）和魔刃（`19746`）优先级高于最终 Boss 加特勒（`19626`）。配置迁移只追加缺失目标，并按此要求调整这两个 ID 相对于加特勒的位置。

| 节点 | 坐标 | 类型 | BOSS BaseId |
|---:|---|---|---|
| 0 | `(-700.000, 0, 0.000)` | Start | |
| 1 | `(-700.005, 0, -9.031)` | Battle | `19539` 奇子·曼提克 |
| 2 | `(-700.020, 0, -17.779)` | Shop | |
| 3 | `(-704.988, 0, -25.317)` | Battle | `19540` 奇子·牛头魔；`19543` 奇子·冥鬼之眼 |
| 4 | `(-694.910, 0, -25.403)` | Battle | `19544` 奇子·双足飞龙 |
| 5 | `(-704.886, 0, -34.211)` | Treasure | |
| 6 | `(-694.862, 0, -34.268)` | Rest | |
| 7 | `(-699.921, 0, -41.763)` | Battle | `19548` 奇子·虚灵法师；`19549` 奇子·僵尸 |
| 8 | `(-699.928, 0, -50.748)` | Treasure | |
| 9 | `(-700.021, 0, -59.706)` | Battle | `19551` 奇子·牛魔老哥；`19552` 奇子·牛魔老弟 |
| 10 | `(-704.830, 0, -67.459)` | Battle | `19553` 奇子·恶魔；`19554` 奇子·恶魔兵装；`19555` 奇子·小恶魔 |
| 11 | `(-694.970, 0, -67.240)` | Treasure | |
| 12 | `(-699.892, 0, -74.832)` | Shop | |
| 13 | `(-700.082, 0, -83.978)` | Boss | `19557` 寻兽探奇 路斯福洛克斯；`19558` 小地豆 |

## 7. 移动和导航

`VnavmeshClient` 负责封装公开的 vnavmesh 接口：

- 检查导航网格是否就绪。
- 调用 `Path.MoveTo` 移动到目标坐标。
- 调用 `Path.Stop` 停止移动。
- 查询路径是否正在运行。

当前使用 `Path.MoveTo` 直线移动，不使用 `SimpleMove.PathfindAndMoveTo`。

`MoveTo` 负责：

- 记录导航开始时间。
- 避免重复启动同一个导航任务。
- 处理导航提前结束。
- 限制连续重试次数。
- 到达节点后停止导航并进入事件处理。

玩家处于棋盘连接桥上、暂时没有节点编号时，控制器会保留前往目标节点的移动，不把桥段误判为战斗准备。该保护仅在 `Board` 或 `Loading` 阶段生效；如果已经识别为 `PartySetup`，即使当前位置尚未进入节点的 `1.25` 米识别范围，也必须停止导航并优先处理编队界面。

## 8. 战斗处理

### 8.1 编队

兽笛固定顺序：

```text
1号：螳螂
2号：胡蜂
3号：库西
```

`PartySetupAction` 读取当前队伍并按需要逐项调整。`XBMPetParty` 的槽位值为 `0=I号兽笛`、`1=II号兽笛`、`2=III号兽笛`、`3=待命`。所有编队决策使用稳定的 `PetId`：缺失检查、存活检查、下一只选择和兽笛槽位完成判断均不比较本地化名称；名称只用于 UI 和日志显示，因此兼容简体、繁体及翻译差异。配置中的魔兽死亡后，编队会从当前存活的待命魔兽中优先选择替补自动补入对应槽位，不修改配置；如果目标槽位已经有存活替补，则优先保留该替补。替补候选会排除所有仍存活的配置魔兽；编队开始时固定本轮三个目标 ID，战斗开始检查复用这组实际阵容，不会重新要求死亡的配置魔兽。调整时只处理第一个错误槽位的目标宝宝或当前占用者，包括仍占据兽笛槽位的死亡魔兽；先卸下占用者，再装入固定替补，不再清空整个后缀。编队过程中使用 `assigningParty` 阻止自动选中 Boss 和自动接近，避免两个动作互相干扰。

### 8.2 开始战斗

`BattleAction.TryStartBattle` 对 `XBMStageDetailList` 发送注册事件，当前使用 `FireCallback` 的回调参数 `8` 启动节点战斗。

不得使用回调 `25/8`，该组合会打开解说界面而不是开始战斗。

### 8.3 Boss

`BossAction` 使用 `DataId/BaseId` 匹配 Boss，不使用 `NameId`：

```text
19334  骑士
14531  奇子·骑士
19335  主教
14532  奇子·主教
14567  奇子·主教
19598  奇子·游侠骑士
19600  奇子·主教
19338  夺灵魔
19339  祸蛛蝎
19341  食人魔
19342  妖火
19344  魅惑女妖帕德索
19924  大妖火
```

高段第一盘已确认的战斗目标：

```text
19638  奇子·博学林鸮
19641  高段第一盘节点2战斗目标
19645  奇子·尸生花
19650  奇子·石像鬼
19661  奇子·树精
19662  奇子·蛞蝓
19663  奇子·花苗
19672  怨毒龙 博尔格尼（最终 Boss）
```

高段第二盘已确认的战斗目标按自动选中优先顺序为：

```text
19678  节点2 奇子·毒性蘑菇
19677  节点2 奇子·夺心魔
19675  节点3 奇子·佛劳洛斯
19676  节点3 雷元精
19683  节点7 奇子·深瞳
19682  节点7 奇子·夜魔人
19684  节点7 奇子·爆弹怪
19685  节点7 光元精
19687  节点8 奇子·红格雷姆林
19688  节点8 奇子·格雷姆林
19692  节点8 奇子·石像魔
19689  节点8 奇子·软糊怪
19690  节点8 奇子·奶冻怪
19691  节点8 奇子·甜羹怪
19686  节点8 奇子·阿托莫斯
19693  节点8 奇子·威胁扎哈克
19696  节点9 奇子·火蛟
19698  节点9 奇子·棘鼹
19701  节点9 奇子·蓝闪蝶
19700  节点9 奇子·狱蟾蜍
19704  节点11 奇子·转盘堡
19702  节点11 奇子·杜尔迦
19706  节点13 奇子·拉米亚
19707  节点13 奇子·独眼巨人
19705  节点13 奇子·美杜莎
19708  节点17 奇子·奇美拉
19719  节点18 凝胶化火焰（大）
19717  节点18 凝胶化雷电（大）
19720  节点18 凝胶化火焰（小）
19718  节点18 凝胶化雷电（小）
19716  节点18 奇子·独眼巨人
19715  节点18 奇子·巨人
19710  节点19 奇子·斯芬克斯
19730  节点20 奇子·莫古小贼
19724  节点20 奇子·莫古大医
19723  节点20 奇子·莫古大术
19727  节点20 奇子·莫古大剑
19721  节点20 奇子·莫古小剑
19722  节点20 奇子·莫古小猛
19725  节点20 莫古小术
19726  节点20 奇子·莫古小医
19728  节点20 奇子·莫古小歌
19733  节点23 雷元精
19732  节点23 奇子·铁巨人
19731  节点23 奇子·贝希摩斯
19737  节点25 死亡沙漏
19741  节点25 奇子·肮脏之眼
19740  节点25 奇子·哈帕利特
19736  节点25 奇子·冥鬼之眼王
19748  节点26 奇子·塔纳托斯
19746  节点26 魔刃
19742  节点26 魔斧之主 劳妲
```

节点26中奇子·塔纳托斯（`19748`）和魔刃（`19746`）的优先级高于魔斧之主劳妲（`19742`）；第三盘的最终 Boss 加特勒（`19626`）同样排在这两个目标之后。其余默认优先级按采集顺序保存，不按数值或名称重新排序。

自动选中使用“BOSS选择”配置中的明确 BaseId 优先级，不依赖 ObjectTable 的枚举顺序。自动选中、自动接近、距离计算和 Boss 信息显示使用同一目标选择规则。默认列表保留已采集目标，用户可调整顺序、删除或添加名称与 ID。

自动接近开启时：

1. 查找可选中的 Boss。
2. 距离按“玩家中心到目标命中圈边缘”计算：`max(0, 中心距离 - 目标 HitboxRadius)`，不扣除玩家自身半径。
3. 圈边距离大于20米时，移动到距目标命中圈边缘约18米的位置。
4. 目标实际出现且圈边距离不超过20米后开始独立计时，稳定等待5秒。
5. 发送基础设置中的 `CountdownCommand`，默认 `/beastmaster countdown 10`；失败时不标记完成，稍后重试。

自动接近关闭时，不自动移动或发送倒计时。

## 9. 奖励、结算和重新开始

确认框按 `ArenaPhase`、业务 Addon 和状态机判断：战利品、商店、宝箱、休息和挑战确认均不依赖本地化文字。插件主动发送挑战并设置 `challengeSent` 后，在 `Entry/PartySetup` 中出现 `SelectYesno` 时点击“是”；自动编队过程中出现提示时由编队流程点击“否”；其他未知确认窗口不处理。入口确认不使用 `PetId/Slot/Hp` 决定“是/否”。`PromptAction` 不再包含简体中文确认规则。

入口菜单、出发按钮和入口 NPC 无法只凭阶段唯一确定，因此使用安全的多语言别名匹配，覆盖简体、繁体、日文和英文；繁体入口支持 `挑戰「鬥獸奇奕」`，日文入口匹配 `闘獣練`、`この盤面に挑む` 和 `盤面に挑`，出发按钮支持 `突入`。匹配前先排除中断数据、魔兽图鉴、询问说明、对话和取消项目；匹配失败时保持等待并在错误信息中列出实际菜单，不点击未知项目。日服的 `はい/いいえ` 通过 `SelectYesno` 回调处理，不依赖文字。休息按钮支持“休息/休憩/休息する/休む/Rest/Take a Rest”；离开休息区通过注册事件处理，不依赖 `立ち去る` 文本。商店备用识别支持繁体 `請選擇要購買的臨時道具` 和 `可以販賣當前持有的道具`。商店购买导致的道具替换优先通过 `XBMContentsItemShop + XBMContentsItemDispose` Addon 组合识别，文本仅作兼容回退。

### 9.1 普通奖励

战斗结束后依次处理：

- `XBMContentsBooty`：优先发送“全部获取”。当 `Loot` 阶段同时出现 `XBMContentsBooty` 和 `SelectYesno` 时，基于 Addon 上下文直接确认，不依赖简体、繁体、日文或英文提示文本。如果 `SelectOk` 提示奇弈道具将超出上限，则关闭提示、停止重复发送“全部获取”，改为选择第一件战利品并等待确认或进入道具替换。
- `XBMContentsTreasure`：读取候选奖励和当前持有道具。当前没有恢复类道具时，按奇弈道具购买优先级选择恢复类候选；已有恢复类道具时，按斗兽装备购买优先级选择装备候选；没有匹配候选时回退第一张卡片。装备栏持有盗贼匕首（ID `57`）时会连续选择两件奖励；第一件选择后重新读取更新后的候选列表，再按相同优先级选择第二件。`SelectOk` 出现后按刚才选择的内部物品类别区分处理：奇弈道具按容量受限处理，斗兽装备按重复装备处理，不依赖本地化提示文字。重复装备会被排除、回滚本次选择计数并按相同优先级选择下一件，直到成功或没有候选。
- `XBMContentsItemDispose`：非商店奖励导致道具栏已满时进入清理状态，按槽位顺序选择第一个未保护道具，并自动确认用新奖励替换旧道具。没有未保护道具时停止流程。
- `XBMContentsItemShop`：按 `SkipShop` 设置直接离开，或执行采集、出售、装备购买和奇弈道具购买流程。
- 休息界面：按 `SkipRest` 设置离开或停留。

休息节点处理：

- 启用 `SkipRest` 时直接点击“离开”。
- 未启用 `SkipRest` 时点击“休息”。
- 识别“只让玩家休息”且包含“恢复”和“体力”的 `SelectYesno` 确认框并选择“是”，兼容90%、100%等不同恢复比例。
- 确认休息后点击“离开”，再继续棋盘路线。

### 9.2 最终结算

发现 `ArenaPhase.Result` 后：

1. 停止导航。
2. 等待结算界面稳定5秒。
3. 点击结算“下一页”。
4. 等待3秒。
5. 再次触发结算按钮关闭界面；失败时每秒重试。
6. 不再执行棋盘节点处理。
7. 清空当前轮的节点、事件、战斗和奖励状态。
8. 等待离开当前斗兽副本区域。
9. 离开副本区域后等待2秒，再从入口流程开始下一轮。

`waitingForAreaExit` 和 `wasInResult` 用于避免结算界面消失、Phase 切换和地图切换不同步时误执行节点12导航。

“继续”操作支持按事件界面恢复节点：商店界面匹配 Shop 节点，休息界面匹配 Rest 节点，宝箱或道具清理界面匹配 Treasure 节点。事件场景中玩家坐标不要求位于节点识别半径内；同类型只有一个节点时直接使用，有多个候选时按当前坐标选择最近节点。恢复事件节点后继续处理当前界面，不会提前把该节点标记为已完成。

## 10. UI 设计

`MainWindow` 当前从上到下依次为：操作按钮、设置、状态摘要、流程信息、调试信息。

### 当前状态

显示：

- 插件版本。
- 当前阶段。
- 区域和区域编号。
- 当前节点。
- Boss 信息。

窗口标题栏格式：

```text
ArenaPilot-斗兽塔助手 v0.2.4.12
```

### 流程日志

保留当前插件会话内所有发生变化的 `FullFlowStatus`，用于追踪完整流程历史。每行格式为 `[HH:mm:ss] 状态内容`，新增状态不会删除旧记录。日志区域固定3行高度，新状态到达时自动滚到底部，用户可以向上滚动查看历史。标题旁的“复制全部”按钮将完整流程历史写入剪贴板。插件重新加载后历史从空白开始。

### 操作

第一行四个主按钮：

```text
开始  继续  停止  状态
```

按钮颜色：

- `开始`：绿色。
- `继续`：黄色。
- `停止`：红色。
- `状态`：橙色。

第二行辅助控件：

```text
配置  记录坐标  节点编号  节点类别
```

- `配置`：打开兽笛与目标盘配置窗口。
- `记录坐标`：将手动选择的节点编号、节点类别和当前玩家坐标追加到剪贴板（用于采集新盘节点）。
- 节点编号支持 `0~29`，节点类别支持起点、战斗、宝箱、休息、商店、BOSS 和随机。

四个主按钮使用统一宽度和共享 `FramePadding`，保证高度一致。

点击 `状态` 按钮会将完整诊断状态复制到剪贴板。复制文本首行显示程序集版本，例如：

```text
版本：v0.2.4.12
状态：PartySetup
斗兽阶段：正在编队准备战斗
...
```

### 诊断与采集

`记录坐标` 属于临时诊断/采集功能，**结果直接写入系统剪贴板，不落地到文件**：

- `记录坐标`：每次点击在剪贴板末尾追加一行（时间、当前路线、X/Y/Z、节点和类型），连续点击可累积多个节点。
- `状态` 只复制常规运行状态，不附加原始界面数据。
- 道具清理、商店和确认窗口的原始界面数据通过独立的“采集数据”按钮复制到剪贴板。

匿名快照导出（`ExportSnapshot`）仍按原逻辑写入诊断目录。

商店协议采集分为手动诊断和自动目录采集：

- “采集数据”输出商店商品槽位、类别、ID、名称、价格、打折、已购买状态、卡片 NodeId，以及当前奇弈道具栏和斗兽装备栏。
- 启用 `AutoCollectShopItems` 后，只要商店打开，控制器每秒扫描一次并写入插件配置目录下的 `ArenaShopItems.csv`。
- CSV 字段为 `类别,ID,名称`，按 ID 去重，使用 UTF-8 BOM；“打开采集文件夹”可直接定位该文件。
- 自动采集逐个调用列表组件的 `ScrollToItem`，按“滚动、等待一轮、读取”遍历全部商品。自动流程等待完整扫描结束并回到顶部后才开始出售和购买。
- 商品名称优先从逻辑槽位对应的 `GetItemRenderer(slot)` 实际渲染器读取，固定 NodeId 路径仅作为兼容回退。

插件内置完整的 203 条斗兽奇弈物品目录：斗兽装备 ID `1-75`、奇弈道具 ID `76-143`、食料 ID `144-203`。内置名称用于 UI、状态日志和采集回退，并作为已知 ID 的权威名称；未来未知 ID 仍可写入 CSV。

斗兽装备和奇弈道具使用两套独立栏位。奇弈道具栏从 AtkValue 索引154开始、每格5个字段；斗兽装备栏从索引205开始、每格5个字段。装备栏槽位块存在即计入容量，首 Bool 仅表示当前是否占用。购买前每次重新读取金币、商品状态和两个栏位。

如果客户端中的商店 Addon 名称与预期不一致，读取器会通过“请选择要购买的临时道具”或“可以贩卖当前持有的道具”等窗口文本识别商店；状态文本同时列出全部可见 Addon，并附加识别到的实际商店 Addon 数据。

商品点击后出现“确定要购买『商品名』吗？”确认框时，流程精确匹配“确定要购买”和“吗？”并选择“是”。购买确认要求金币减少、商品槽位已购买标志为 true，且对应道具栏或装备栏出现目标 ID。出售确认要求目标 ID 的持有数量减少且金币增加。

未启用 `SkipShop` 时，商店状态机固定为：

```text
完整滚动采集商品（启用自动采集时）
  -> 按槽位出售所有未保护奇弈道具
  -> 按装备购买优先级购买余额足够且尚未持有的装备
  -> 按奇弈道具购买优先级购买余额足够的道具
  -> 没有目标或栏位已满时退出商店
```

商店不会出售斗兽装备。每笔交易确认后会单独保留一轮流程状态，日志记录物品名称、收入或支出；没有找到优先购买且余额足够的装备时也会明确记录。购买/出售确认超时为20秒。

### 设置

当前设置：

- 跳过商店。
- 跳过休息。
- 自动选中 Boss。
- 自动接近+倒计时。
- 斗兽装备购买优先级。
- 奇弈道具购买优先级。
- 奇弈道具保护列表。

主窗口中的每个设置复选框都有悬停提示，说明启用和关闭后的实际行为及关键限制，包括商店/休息是否直接跳过、BOSS 自动选中依据，以及自动接近的20米阈值与倒计时。

配置窗口使用四个顶层页签：

- `基础设置`：目标盘和三个兽笛配置。
- `商店策略`：装备购买、道具购买和保护列表。
- `BOSS选择`：显示自动选中 BaseId 优先列表，支持置顶、置底、上移、下移、删除、名称与 ID 添加、剪贴板导入导出和恢复默认。
- `路线选择`：已实现路线的可视化和自定义编排。

路线选择页内每张已实现路线使用独立子页签。路线图显示所有已收录节点和连接，节点颜色表示起点、战斗、商店、休息、宝箱、随机和 Boss；默认首选路径、自定义路径和普通连接使用不同线条颜色。未采集坐标的节点以灰色显示，悬停提示“坐标未采集”，不能加入自定义路线。已采集节点悬停显示编号、类型和坐标。

自定义路线通过依次点击节点编排，只允许沿节点的 `Next` 连接追加。点击已在草稿中的节点会将路线截断到该节点。编辑器提供撤销、恢复默认、保存路线和设为目标盘。配置按 Stage ID 保存节点序列；运行时只在自定义序列提供有效相邻连接时覆盖默认下一节点，未覆盖或非法的部分回退默认路线。

装备购买、道具购买和保护列表使用三个横向页签。每个页签支持按中文名称或数字 ID 搜索并快速添加；每项提供置顶、置底、上移、下移和删除操作，修改后立即保存。装备列表只显示 ID `1-75`，奇弈道具列表只显示 ID `76-143`。保护列表优先于商店清理和宝箱替换。

三个页签各自支持剪贴板导入和导出。导出格式为 `优先级,ID,名称` CSV；导入兼容该 CSV，也兼容使用逗号、空格或换行分隔的纯 ID 列表。导入时按当前页签允许的 ID 范围过滤、保持输入顺序并去重，然后整体替换当前列表。

鼠标悬停在已配置条目或搜索下拉选项上时显示物品提示，包括名称、内部 ID、类别、Wiki 售价和完整效果词条。例如治愈戒指显示售价175和“战斗结束时恢复玩家25%的体力”。装备与奇弈道具共143条词条内置于 `CrucibleItemDetails`。

策略列表在 JSON 反序列化时使用整体替换，避免将已保存内容追加到默认列表。插件初始化时会按原顺序自动去重并保存修复后的配置；同一道具在同一个策略列表中最多出现一次。

默认装备购买优先级排除明确带有减速、降低体力、自身异常或死亡宣告风险的装备，优先高体力、高伤害和高减伤装备，例如英雄王冠、大师盾、烈风铠甲、秘银铠甲、星极上衣和盗贼上衣。默认奇弈道具购买顺序为恢复药套装、4/3/2/1级恢复药、3/2/1级药粉和魔兽吸血药。金币不足时跳过当前目标并检查后续目标。

六种属性斧（ID `38-43`）属于互斥装备组。当前装备栏已经持有任意属性斧后，商店购买和宝箱选择均跳过其余属性斧，整轮最多持有一把。判断基于装备栏实际持有 ID，因此跨多个商店继续生效。

默认保护列表包括：1至4级魔兽恢复药、1至3级魔兽药粉、六属性牙、吸血鬼之牙、魔兽恢复药套装和魔兽治愈套装。商店按槽位出售所有其他奇弈道具；宝箱满栏时按槽位替换第一个未保护道具。所有道具都受保护时停止替换，避免误删。

商店购买或出售请求后等待界面确认，超时时间为20秒。状态文本显示当前商店动作、目标道具和槽位；超时会停止流程并在原因中明确区分购买或出售，避免重复点击造成错误交易。

商店商品区使用 `XBMContentsItemShop` 的列表组件（NodeId 5）按商品槽位执行 `SelectItem` 和 `DispatchItemEvent`。不直接依赖 `31000 + 槽位` 的卡片节点，因为滚动区域中未显示的商品卡片不会实例化点击事件。

调试信息默认折叠，展开后显示阶段、位置、节点、路线、Addon 和动作状态。兽笛自动编队每次点击前记录所有成员的 `PetId/HP/Slot` 快照；只有观察到快照变化后才允许下一次点击，防止界面尚未刷新时重复点击导致替补被取消。等待期间“兽笛操作”显示完整成员快照；点击状态同时显示本轮固定目标，便于实机诊断。

## 11. 配置项

配置保存在 Dalamud 插件配置目录，由 `Configuration` 管理：

| 配置项 | 作用 |
|---|---|
| `SkipShop` | 开启时直接离开商店；关闭时执行采集、道具清理、装备购买和道具购买 |
| `SkipRest` | 自动离开休息区 |
| `AutoTargetBoss` | 战斗准备阶段自动选中 Boss；关闭时清除当前已知 Boss 目标，其他插件仍可能自行选怪 |
| `AutoApproach` | 自动接近 Boss，并在接近后发送倒计时 |
| `RepeatCount` | 完成 X 场后停止；`0` 表示不限场数持续循环 |

主窗口在“完成 X 场停止”输入框右侧显示当前进度：运行中显示“当前第 N 场”，停止后显示已完成场数，未启动时显示“尚未开始”。场次在最终结算真正关闭后累加，达到上限时状态切换为 `Completed`，不再启动下一轮。
| `CountdownCommand` | 自动接近后发送的可编辑倒计时指令，默认 `/beastmaster countdown 10` |
| `AutoCollectShopItems` | 商店打开时自动滚动并维护 `ArenaShopItems.csv` |
| `EquipmentPurchasePriority` | 斗兽装备购买优先级 |
| `ShopPurchasePriority` | 奇弈道具购买优先级 |
| `ProtectedItemIds` | 商店出售和宝箱替换均不得处理的奇弈道具 |
| `BossPriority` | 按顺序保存 BOSS 名称和 BaseId；用于自动选中、自动接近、距离及目标信息判断 |
| `CustomRoutes` | 按 Stage ID 保存的自定义节点序列；有效连接覆盖默认路线 |

## 12. 安全和故障处理

出现以下情况时应停止当前动作或等待，不得盲目继续：

- vnavmesh 尚未就绪。
- 目标 Addon 不存在或未就绪。
- 按钮不可用。
- 场地、区域或关卡编号不匹配。
- 导航连续失败。
- 战斗开始在限定时间内未确认。
- 节点事件超时。
- 结算后区域切换未完成。
- 读取到未知阶段或遮挡界面。

停止总流程时必须调用导航停止，并保留可读的停止原因。

## 13. 构建和迁移

### 构建

在新目录 `E:\git\ArenaPilot` 执行：

```powershell
dotnet build ArenaPilot.csproj -c Release
```

构建产物：

```text
E:\git\ArenaPilot\output\ArenaPilot.dll
```

### 配置迁移

`Configuration.Version` 当前为 `14`。插件初始化时执行一次性迁移：

- 版本低于2时写入默认斗兽装备购买优先级。
- 版本低于3时写入当前默认保护列表，并移除代码对旧出售/替换优先级的依赖。
- 版本低于4时写入新版装备购买优先级：治愈戒指优先，其次为六种属性斧，再按生存和伤害装备排序。
- 版本低于5时关闭 `AutoCollectShopItems`；主界面不再显示自动采集开关和采集文件操作按钮。
- 版本低于6时关闭 `SkipShop` 和 `SkipRest`，默认完整处理商店和休息阶段。
- 版本低于7时写入默认倒计时指令 `/cd 5`。
- 版本低于8时将默认倒计时指令更新为 `/驯兽师 倒计时 10`。
- 版本低于9时将完成场数上限设为 `0`，默认持续循环。
- 版本低于10时将默认倒计时指令更新为 `/beastmaster countdown 10`。
- 版本低于11时写入默认 BOSS 名称与 BaseId 优先列表。
- 版本低于12时将第二盘 BOSS BaseId 追加到现有优先级列表，不覆盖用户顺序。
- 版本低于13时将奇子·塔纳托斯和魔刃移至加特勒之前，其余用户优先级顺序保持不变。
- 版本低于14时将魔刃的错误 ID `19476` 修正为 `19746`，尽量保留原列表位置。
- 所有列表按原顺序移除无效 ID 和重复 ID，发生修复时立即保存。

旧 JSON 中残留的已删除字段由反序列化器忽略，不影响当前流程。用户迁移完成后对优先级或保护列表的手动修改不会被重复覆盖。

## 14. 当前已知限制

- 当前路线实现第一盘、第二盘、第三盘、高段第一盘和高段第二盘固定路线。第二盘默认路线为 `0 -> 1 -> 2 -> 3 -> 5 -> 7 -> 8 -> 9 -> 11 -> 12 -> 13`，坐标、节点类型和已知 BOSS BaseId 已录入，仍需持续实机验证完整流程。
- 商店自动购买斗兽装备和奇弈道具，食料仅采集、不购买；装备栏满时跳过剩余装备购买。
- 战利品和寻宝在满栏时会改为单件领取并进入替换流程，该 Addon 事件协议仍需随客户端版本持续实测。
- 战斗过程依赖外部战斗插件或玩家操作。
- vnavmesh 接口和游戏 Addon 可能随客户端版本变化。
- 结算、节点和入口行为仍需要在游戏内持续验证。
- 该自动化功能可能涉及游戏服务条款和账号风险，使用者需自行承担风险。
